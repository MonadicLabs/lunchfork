# No ML dependencies (no pytorch, no onnxruntime)
numpy>=1.24
structlog>=23.0
pydantic>=2.0
paho-mqtt>=1.6
# SoapySDR is installed as a system package, not via pip
# gpsd-py3 for GPS daemon integration
gpsd-py3>=0.3.0
pyserial>=3.5
